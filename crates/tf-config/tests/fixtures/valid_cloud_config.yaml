# Valid cloud mode configuration example
# This fixture demonstrates a complete cloud LLM configuration

project_name: "cloud-test-project"
output_folder: "./output"

jira:
  endpoint: "https://jira.example.com"
  token: "jira-token"

squash:
  endpoint: "https://squash.example.com"
  username: "testuser"
  password: "secret-password"

templates:
  cr: "./templates/cr.md"
  ppt: "./templates/ppt.pptx"
  anomaly: "./templates/anomaly.md"

llm:
  mode: "cloud"
  cloud_enabled: true
  cloud_endpoint: "https://api.openai.com/v1"
  cloud_model: "gpt-4o-mini"
  api_key: "sk-test-api-key"
  timeout_seconds: 120
  max_tokens: 4096
